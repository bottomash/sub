common:
  api_mode: false
  clash_rule_base: sparkle/clash.yml
  enable_filter: true

node_pref:
  sort_flag: false
  clash_use_new_field_name: true
  clash_proxies_style: flow
  rename_node:
  - {import: rename.txt}

aliases:
  - {uri: /clash, target: "/sub?target=clash&new_name=true"}

advanced:
  enable_cache: false