{
  "name": "sub-config",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-configs.mjs",
    "check": "node build-configs.mjs --check"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
